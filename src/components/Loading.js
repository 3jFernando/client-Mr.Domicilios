import React from 'react';

export default function Loading(props) {
  return (
    <div className="alert alert-dark bg-dark">
      <b className="text-white">Cargando, por favor espera...</b>
    </div>
  );
}